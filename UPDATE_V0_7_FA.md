# نسخه 0.7.0 — مرکز عملیات روزانه

این نسخه به‌صورت آپدیت روی نسخه قبلی نصب می‌شود و داده‌های موجود را حذف نمی‌کند.

## تغییرات اصلی
- داشبورد به مرکز عملیات امروز تبدیل شده و چهار شاخص بالایی قابل لمس هستند.
- نوار پایین: خانه، امروز، گزارش‌ها، سوابق؛ «واحدها» از دسترسی روزمره حذف شده است.
- دسترسی سریع: امروز، جستجوی سراسری، تقویم اشغال.
- صفحه امروز با فیلتر ورودی، خروجی، حاضر و رزروهای فعال.
- ثبت واقعی «ورود انجام شد» و «خروج انجام شد» برای هر رزرو.
- تقویم هفتگی اشغال تمام واحدها؛ روز خروج آزاد محسوب می‌شود.
- جستجوی سراسری با نام/فامیل/موبایل/کد ملی و نمایش رزروها و سوابق.
- صفحه واحدها فقط نمایشی باقی مانده و ظرفیت از UI قابل تغییر نیست.
- مشخصات نفر اصلی و همراهان برای هر واحد قابل ثبت/ویرایش است؛ کد ملی و موبایل اختیاری‌اند.
- ویرایش تاریخ، واحد، تعداد نفرات، تمدید، اضافه/حذف واحد کاروانی و لغو رزرو حفظ شده است.

## دیتابیس
قبل از نصب APK، `migration_v0_7.sql` را یک‌بار در Supabase SQL Editor اجرا کنید.
Migration فقط ستون‌های check-in/check-out و RPCهای جدید را اضافه می‌کند و رکوردهای قبلی را حذف نمی‌کند.
