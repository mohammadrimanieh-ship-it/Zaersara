# ساخت APK بدون Android Studio

این پروژه طوری تنظیم شده که GitHub Actions فایل APK را به‌صورت خودکار بسازد.

## فقط برای بار اول
1. در GitHub یک Repository جدید و ترجیحاً Private بسازید.
2. تمام فایل‌های داخل همین پوشه پروژه را در Repository آپلود کنید.
3. وارد تب **Actions** شوید.
4. Workflow با نام **Build Android APK** را باز کنید.
5. روی **Run workflow** و سپس دوباره **Run workflow** بزنید.
6. بعد از سبز شدن Build، همان صفحه را باز کنید.
7. پایین صفحه در بخش **Artifacts** روی **ZaersaraMashhad-v0.1.0-APK** بزنید.
8. فایل ZIP دانلود می‌شود؛ داخل آن فایل `ZaersaraMashhad-v0.1.0.apk` قرار دارد و مستقیماً روی گوشی نصب می‌شود.

## آپدیت‌های بعدی
هر بار نسخه جدید پروژه روی شاخه main/master قرار بگیرد، GitHub به‌صورت خودکار APK جدید می‌سازد. برای نصب نسخه جدید روی نسخه قبلی باید `applicationId` ثابت بماند و نسخه با همان کلید امضا ساخته شود. نسخه فعلی Debug برای تست اولیه است؛ قبل از استفاده نهایی چندکاربره بهتر است Release signing ثابت نیز فعال شود.

## اتصال آنلاین برنامه
بعد از نصب، برنامه در اولین اجرا آدرس Supabase و Anon Key را می‌خواهد. SQL داخل `backend/schema.sql` باید یک بار در Supabase اجرا شود و کاربران از بخش Authentication در Supabase ساخته شوند.
