# تغییرات نسخه 0.5.0 زائرسرا مشهد

این نسخه ادامه مستقیم v0.4.0 است و اطلاعات قبلی Supabase را حذف نمی‌کند.

## مهم: ترتیب نصب
1. ابتدا فایل `backend/migration_v0_5.sql` را در Supabase SQL Editor اجرا کنید.
2. سپس Patch نسخه 0.5.0 را روی GitHub آپلود و Commit کنید.
3. از GitHub Actions فایل APK Release را بسازید و روی نسخه قبلی نصب کنید.

## تغییرات اصلی
- طراحی جدید تیره سرمه‌ای/بنفش مطابق طرح تأییدشده، کارت‌های گرد، آیکون‌های رنگی و انیمیشن ملایم.
- داشبورد جدید با تاریخ شمسی، ساعت زنده، وضعیت انیمیشنی اتصال سرور و ناوبری پایین.
- کارت رزروهای نزدیک قابل لمس است و صفحه جزئیات رزرو باز می‌شود.
- تقویم ورود/خروج در یک کارت؛ ابتدا ورود و سپس خروج انتخاب می‌شود.
- روز خروج اشغال نیست؛ رزرو بعدی می‌تواند همان روز وارد همان واحد شود (`[start,end)`).
- هر واحد در یک بازه فقط متعلق به یک خانواده/کاروان است؛ ظرفیت خالی یک رزرو به خانواده دیگری داده نمی‌شود.
- پیشنهاد کاروانی داخل یک مجموعه و با اولویت زائرسرا، سپس فاطمیه، سپس آپارتمان.
- نمایش ظرفیت آزاد هر مجموعه هنگام پیشنهاد کاروانی.
- بعد از انتخاب پیشنهاد، دکمه واضح «ادامه و ثبت اطلاعات» نمایش داده می‌شود.
- برای هر واحد فقط نام خانوادگی اصلی + تعداد نفرات الزامی است؛ ثبت تک‌تک زائران حذف شده است.
- گزارش مکانی نام خانوادگی اصلی هر رزرو را نشان می‌دهد.
- جزئیات رزرو + ویرایش اطلاعات، تغییر تاریخ، تغییر واحد، تغییر تعداد نفرات/نام خانوادگی، تمدید، لغو، افزودن/حذف واحد کاروانی.
- لغو رزرو حذف واقعی نیست و وضعیت رزرو به cancelled تغییر می‌کند.
- جستجوی سوابق نام خانوادگی/موبایل، رزروهای ساده‌شده نسخه جدید را هم پوشش می‌دهد.
- versionCode=9 و versionName=0.5.0؛ applicationId و امضای Release تغییر نکرده‌اند.
