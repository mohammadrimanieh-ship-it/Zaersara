# به‌روزرسانی زائرسرا مشهد به 0.2.0

## تغییرات نسخه
- انتخاب تاریخ ورود و خروج با تقویم شمسی
- غیرفعال بودن تاریخ‌های گذشته
- امکان افزودن چند زائر در یک رزرو
- کد ملی ۱۰ رقمی اجباری برای هر نفر
- تکمیل خودکار نام و نام خانوادگی افراد قبلی با وارد کردن کد ملی
- نمایش تعداد دفعات اقامت فرد قبلی
- کنترل ظرفیت: سوئیت‌های ۵ نفره حداکثر ۵، سوئیت‌های ۶ نفره حداکثر ۶ و سالن حداکثر ۱۴ نفر
- دکمه ثبت رزرو همراه با دلیل واضح در صورت ناقص بودن اطلاعات
- versionCode=2 و versionName=0.2.0

## دیتابیس
در Supabase > SQL Editor فایل `backend/migration_v0_2.sql` را یک‌بار Run کنید. این فایل اطلاعات قبلی را حذف نمی‌کند.

## نکته بسیار مهم درباره آپدیت APK
نسخه 0.1.0 با Debug Key موقت GitHub Actions ساخته شده بود. GitHub Runner در هر اجرای تازه ممکن است Debug Key جدید بسازد؛ بنابراین Android ممکن است APK 0.2.0 را مستقیماً روی 0.1.0 نصب نکند.

برای اینکه از 0.2.0 به بعد آپدیت‌ها همیشه روی نسخه قبلی نصب شوند، باید یک Release Keystore ثابت برای GitHub Actions تنظیم شود. در اولین انتقال به امضای ثابت ممکن است لازم باشد نسخه 0.1.0 را یک‌بار حذف و 0.2.0 را نصب کنید. اطلاعات رزروها در Supabase می‌ماند، ولی URL/Key و ورود دستگاه را دوباره وارد می‌کنید.

از نسخه 0.2.0 به بعد applicationId ثابت `com.imanieh.zaersara` و versionCode افزایشی حفظ می‌شود.
